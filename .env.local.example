# Base URL to your backend API (no trailing slash)
# e.g. https://lms.yourdomain.com
NEXT_PUBLIC_API_BASE_URL=

# Optional: Customize proxy prefix (defaults to /api/proxy)
NEXT_PUBLIC_API_PROXY_PATH=/api/proxy
